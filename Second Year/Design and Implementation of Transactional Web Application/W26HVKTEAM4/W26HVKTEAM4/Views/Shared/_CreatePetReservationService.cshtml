@model W26HVKTEAM4.Models.PetReservation

@{
}

<div>
    <select asp-for="@Model.PetReservationServices">
        @foreach (var pet in Model.PetReservationServices)
        {
            <option asp-for="@Model.Pet" value="@pet" id="@pet">@pet</option>
        }
    </select>
</div>